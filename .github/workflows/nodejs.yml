name: Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - name: Set up node using nvm
      uses: dcodeIO/setup-node-nvm@v2.0.0
    - run: npm install
    - run: npm test
      env:
        CI: true
